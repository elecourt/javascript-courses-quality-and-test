<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meilleur Score du Jour</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 0;
            padding: 0;
        }
        main {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .score-section {
            max-width: 800px;
            margin: 20px;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #ddd;
        }
        h2 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }
        #today-scores ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        #today-scores li {
            padding: 10px;
            border-bottom: 1px solid #ddd;
            transition: background-color 0.3s ease;
        }
        #today-scores li:last-child {
            border-bottom: none;
        }
        #today-scores li:hover {
            background-color: #f1f1f1;
        }
        h2 {
            color: #00796b;
        }
    </style>
</head>
<body>
    <main>
        <div class="score-section">
            <h2>Meilleur Score du Jour</h2>
            <div class="score-list" id="today-scores">
                <ol>
                    <% scores.forEach(function(item) { %>
                        <li><%= item.pseudo %> || <%= item.score %> points</li>
                    <% }); %>
                </ol>
            </div>
        </div>
    </main>
</body>
</html>
